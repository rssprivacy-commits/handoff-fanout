"""Shared pytest fixtures.

Real fixtures (isolated $HOME / fake git repo / mock launchd) land when the
core modules are extracted in v0.2.0+. This stub keeps `pytest tests/` green
during the scaffolding phase.
"""
